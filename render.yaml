services:
  - type: web
    name: matchflow-brain
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn brain.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONPATH
        value: .
